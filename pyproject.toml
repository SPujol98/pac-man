[project]
name = "pacman"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pydantic",
    "pygame"
]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=1.20.0",
    "pytest>=9.0.3",
]

[[tool.mypy.overrides]]
ignore_missing_imports = true